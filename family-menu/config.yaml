---
name: Family Menu
version: "0.1.6"
slug: family_menu
description: Local-first weekly dinner planning, grocery, and prep app
url: https://github.com/kmcrandom/family-menu-ha
image: ghcr.io/kmcrandom/family-menu-ha
arch:
  - aarch64
startup: application
boot: auto
init: false
ingress: true
ingress_port: 8081
panel_title: Family Menu
panel_icon: mdi:food-variant
stage: experimental
ports:
  8081/tcp: 8081
ports_description:
  8081/tcp: Family Menu web app and API exposed on host port 8081
options:
  database:
    path: /data/family-menu.sqlite
  seed:
    path: /app/data/generic-starter-meal-catalog.example.json
    auto_seed: true
  server:
    host: 0.0.0.0
    port: 8081
schema:
  database:
    path: str
  seed:
    path: str
    auto_seed: bool
  server:
    host: str
    port: int
