# Codex Instructions

Use the SDD workflow for all non-trivial changes. Specs live in `specs/`.

Keep product behavior, UX, API, data model, deployment, and security changes reflected in the relevant spec before implementation work. Pause for explicit spec and plan approval at the SDD gates.
